const Discord = require('discord.js')


module.exports = {
    name: "kolejka",
    aliases: [],
    run: async (client, msg, args) => {


let numer = db.get(`numer${msg.guild.id}`)

const embed = new Discord.MessageEmbed()
.setAuthor('Kolejka')

    }
}